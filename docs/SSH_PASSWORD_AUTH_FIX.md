# SSH Password Authentication Fix для Ubuntu 22.04 + Cloud-Init

## 🔴 Проблема

При подключении по SSH к VM на Ubuntu 22.04 получаете ошибку:
```
ubuntu@192.168.200.100: Permission denied (publickey).
```

Парольная аутентификация не работает, несмотря на настройку `ssh_pwauth: true` в cloud-init.

## 🔍 Корневая причина

### Как работает SSH конфигурация в Ubuntu 22.04:

1. **Cloud-init автоматически создает `/etc/ssh/sshd_config.d/50-cloud-init.conf`**
   - Этот файл может содержать `PasswordAuthentication no`

2. **SSH читает конфиги в АЛФАВИТНОМ порядке**
   - Файлы в `/etc/ssh/sshd_config.d/` читаются по порядку: `01-*.conf`, `50-*.conf`, `99-*.conf`

3. **ПЕРВАЯ найденная директива имеет приоритет!**
   - Это ключевой момент! Не последняя, а ПЕРВАЯ!
   - Если `50-cloud-init.conf` содержит `PasswordAuthentication no`, то последующие файлы (например `99-custom-auth.conf`) не смогут это переопределить

4. **Проблема с текущей конфигурацией:**
   - Старая конфигурация создавала `99-custom-auth.conf`
   - Он читался ПОСЛЕ `50-cloud-init.conf`
   - Поэтому настройки из `50-cloud-init.conf` имели приоритет

## ✅ Решение

### Для НОВЫХ VM (исправлено в cloud-init конфигурации):

Изменения в `cloud-init/user-data.yml`:

1. **Создается файл с более высоким приоритетом:**
   ```yaml
   - path: /etc/ssh/sshd_config.d/01-custom-auth.conf  # Вместо 99-custom-auth.conf!
     content: |
       PasswordAuthentication yes
       PermitRootLogin yes
       KbdInteractiveAuthentication yes
       PubkeyAuthentication yes
       UsePAM yes
   ```

2. **Добавлен скрипт для удаления конфликтующего файла:**
   ```yaml
   - path: /usr/local/bin/fix-ssh-config.sh
     content: |
       rm -f /etc/ssh/sshd_config.d/50-cloud-init.conf
       sshd -T | grep passwordauthentication
   ```

3. **В runcmd добавлено выполнение скрипта:**
   ```yaml
   runcmd:
     - /usr/local/bin/fix-ssh-config.sh
     - systemctl restart ssh
   ```

### Для СУЩЕСТВУЮЩИХ VM (быстрое исправление):

**Вариант 1: Использовать готовый скрипт (РЕКОМЕНДУЕТСЯ)**

На вашей локальной машине:
```bash
cd ~/dns_server/examples/local

# Скопировать скрипт на VM (замените ubuntu на root если нужно)
scp -i ~/.ssh/id_rsa ../../scripts/fix-ssh-password-auth.sh ubuntu@192.168.200.100:/tmp/

# Подключиться по ключу и выполнить скрипт
ssh -i ~/.ssh/id_rsa ubuntu@192.168.200.100
sudo bash /tmp/fix-ssh-password-auth.sh
```

**Вариант 2: Ручное исправление**

Подключитесь по SSH-ключу или через консоль libvirt и выполните:

```bash
# 1. Создаем приоритетный конфиг (01- будет первым!)
sudo tee /etc/ssh/sshd_config.d/01-custom-auth.conf > /dev/null <<'EOF'
PasswordAuthentication yes
PermitRootLogin yes
KbdInteractiveAuthentication yes
PubkeyAuthentication yes
UsePAM yes
EOF

# 2. Удаляем конфликтующий cloud-init конфиг
sudo rm -f /etc/ssh/sshd_config.d/50-cloud-init.conf

# 3. Обновляем основной конфиг (опционально)
sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config

# 4. Проверяем синтаксис
sudo sshd -t

# 5. Перезапускаем SSH
sudo systemctl restart ssh

# 6. Проверяем эффективную конфигурацию
sudo sshd -T | grep passwordauthentication
# Должно показать: passwordauthentication yes
```

**Вариант 3: Через virsh console (если SSH недоступен совсем)**

```bash
# На хост-машине подключаемся к консоли VM
sudo virsh console <vm-name>

# Логинимся (username: ubuntu, password: тот что указан в cloud-init)
# Затем выполняем команды из Варианта 2
```

## 🧪 Проверка решения

После применения исправления:

### 1. Проверить эффективную конфигурацию SSH:
```bash
sudo sshd -T | grep -E '(passwordauthentication|permitrootlogin|kbdinteractiveauthentication)'
```

Ожидаемый результат:
```
passwordauthentication yes
permitrootlogin yes
kbdinteractiveauthentication yes
```

### 2. Проверить файлы конфигурации:
```bash
ls -la /etc/ssh/sshd_config.d/
```

Должен быть файл `01-custom-auth.conf` и НЕ должно быть `50-cloud-init.conf`.

### 3. Проверить статус SSH:
```bash
sudo systemctl status ssh
```

### 4. Попробовать подключиться по паролю:
```bash
ssh ubuntu@192.168.200.100
# Введите пароль когда попросит
```

## 📚 Дополнительная информация

### Почему нельзя использовать публичный ключ для подключения?

В вашей ошибке вы пытались использовать:
```bash
ssh ubuntu@192.168.200.100 -i ~/.ssh/id_rsa.pub  # НЕПРАВИЛЬНО!
```

**Проблема:** Вы указываете ПУБЛИЧНЫЙ ключ (`.pub`), а нужен ПРИВАТНЫЙ!

**Правильно:**
```bash
ssh ubuntu@192.168.200.100 -i ~/.ssh/id_rsa  # Без .pub!
```

### Как работает приоритет директив в SSH:

SSH обрабатывает конфигурацию так:
1. Читает `/etc/ssh/sshd_config`
2. Затем читает файлы из `/etc/ssh/sshd_config.d/` в алфавитном порядке
3. **Использует ПЕРВОЕ встреченное значение для каждой директивы**

Пример:
```
01-custom-auth.conf:    PasswordAuthentication yes  ← ЭТОТ ВЫБИРАЕТСЯ!
50-cloud-init.conf:     PasswordAuthentication no   ← Игнорируется
99-other.conf:          PasswordAuthentication yes  ← Игнорируется
```

### Команды для диагностики:

```bash
# Показать эффективную конфигурацию SSH
sudo sshd -T

# Показать только настройки аутентификации
sudo sshd -T | grep -i auth

# Проверить логи SSH
sudo journalctl -u ssh -n 50

# Проверить права на конфиг файлы
ls -la /etc/ssh/sshd_config*

# Проверить синтаксис конфигурации
sudo sshd -t
```

## 🎯 Итоги

**Ключевые моменты для Senior Engineer:**

1. ✅ В SSH конфигурации **ПЕРВАЯ директива имеет приоритет**, не последняя
2. ✅ Используйте **01-** префикс для файлов с наивысшим приоритетом
3. ✅ Удаляйте конфликтующие файлы из sshd_config.d/
4. ✅ Всегда проверяйте эффективную конфигурацию через `sshd -T`
5. ✅ Тестируйте синтаксис через `sshd -t` перед перезапуском
6. ✅ Логируйте изменения для диагностики

## 📖 Ссылки

- [SSH Config Priority Discussion](https://askubuntu.com/questions/1516262/why-is-50-cloud-init-conf-created)
- [Cloud-init SSH Module](https://cloudinit.readthedocs.io/en/latest/reference/modules.html#ssh)
- [Ubuntu 22.04 SSH Issues](https://serverfault.com/questions/1115174/ubuntu-22-10-not-allowing-ssh-with-a-password)

---

**Автор:** Senior DevOps Engineer
**Дата:** 2025-11-21
**Версия:** 2.0 (Fixed priority issue)
