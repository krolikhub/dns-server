# Соображения безопасности при работе с libvirt

## Проблема: Permission denied vs Безопасность

При решении проблемы "Permission denied" существует **компромисс между удобством и безопасностью**.

## Варианты решения и их риски

### ❌ ОПАСНО: Запуск QEMU от root

```bash
user = "root"
group = "root"
security_driver = "none"
```

**Риски:**
- 🔴 **Критический риск**: При наличии уязвимости в QEMU атакующий получает root права
- 🔴 Нарушается принцип минимальных привилегий (principle of least privilege)
- 🔴 Отключение AppArmor убирает дополнительный слой защиты
- 🔴 Виртуальная машина имеет полный доступ к хост-системе

**Когда допустимо:**
- ✅ Изолированная среда разработки
- ✅ Личный компьютер без критичных данных
- ✅ Временное решение для отладки
- ❌ **НЕ для production серверов!**
- ❌ **НЕ для multi-tenant окружений!**
- ❌ **НЕ для систем с важными данными!**

### ⚠️ КОМПРОМИСС: QEMU от непривилегированного пользователя, но без AppArmor

```bash
user = "libvirt-qemu"
group = "libvirt-qemu"
dynamic_ownership = 1
security_driver = "none"
```

**Риски:**
- 🟡 AppArmor отключен, нет дополнительной изоляции
- 🟡 QEMU имеет доступ ко всем файлам пользователя libvirt-qemu

**Преимущества:**
- ✅ QEMU не работает от root
- ✅ Ограниченный доступ к системным файлам
- ✅ Проще настроить, чем полный AppArmor

**Когда допустимо:**
- ✅ Среда разработки
- ✅ Тестовые стенды
- ⚠️ Production с низкими требованиями к безопасности

### ✅ БЕЗОПАСНО: QEMU от непривилегированного пользователя + AppArmor (рекомендуется)

```bash
user = "libvirt-qemu"
group = "libvirt-qemu"
dynamic_ownership = 1
# security_driver = "apparmor" (по умолчанию)
```

**Дополнительно:** Правильная настройка прав на директории

**Преимущества:**
- ✅ QEMU не работает от root
- ✅ AppArmor обеспечивает дополнительную изоляцию
- ✅ Принцип минимальных привилегий соблюден
- ✅ Защита от escape атак из VM

**Недостатки:**
- ⚠️ Требует дополнительной настройки прав на файлы
- ⚠️ Может потребовать настройки AppArmor профилей

**Когда использовать:**
- ✅ **Production серверы**
- ✅ **Multi-tenant окружения**
- ✅ **Системы с критичными данными**
- ✅ **Любые публично доступные серверы**

## Практические рекомендации

### Для локальной разработки (личный компьютер)

Используйте **компромиссный** или даже **небезопасный** вариант, если:
- Вы работаете на своем личном компьютере
- VM используются только для разработки/тестирования
- Нет критичных данных на хосте
- Вы понимаете риски

```bash
sudo ./scripts/fix-libvirt-permissions-safe.sh
# Выберите опцию 2 (Компромиссный) или 3 (Небезопасный)
```

### Для production / публичных серверов

**Обязательно используйте безопасный вариант:**

```bash
sudo ./scripts/fix-libvirt-permissions-safe.sh
# Выберите опцию 1 (БЕЗОПАСНЫЙ)
```

Этот вариант:
1. Запускает QEMU от `libvirt-qemu` (не root)
2. Оставляет AppArmor включенным
3. Автоматически настраивает права на директории
4. Использует `dynamic_ownership = 1` для автоматического управления

### Для контейнеров / WSL2 / вложенной виртуализации

В этих средах AppArmor может работать некорректно. Используйте **компромиссный** вариант:

```bash
sudo ./scripts/fix-libvirt-permissions-safe.sh
# Выберите опцию 2 (Компромиссный)
```

## Дополнительные меры безопасности

### 1. Сетевая изоляция

Если VM не требуют доступа в интернет, используйте изолированную сеть:

```hcl
resource "libvirt_network" "dns_network" {
  name      = "dns-server-network"
  mode      = "none"  # Полная изоляция
  # или
  mode      = "nat"   # Только исходящий трафик
}
```

### 2. Ограничение ресурсов

Ограничьте ресурсы VM для предотвращения DoS на хосте:

```hcl
resource "libvirt_domain" "dns_server" {
  memory = 2048  # Не больше необходимого
  vcpu   = 2     # Не больше необходимого
}
```

### 3. Регулярные обновления

Обновляйте QEMU, libvirt и ядро Linux:

```bash
sudo apt update && sudo apt upgrade -y
```

### 4. Мониторинг

Настройте мониторинг и логирование:

```bash
# Мониторинг libvirt логов
journalctl -u libvirtd -f

# Мониторинг AppArmor (если включен)
sudo aa-status
tail -f /var/log/syslog | grep apparmor
```

## Проверка безопасности после настройки

```bash
# 1. Проверьте, под каким пользователем работает QEMU
ps aux | grep qemu

# 2. Проверьте security model
virsh dominfo dns-server | grep -i security

# 3. Проверьте AppArmor статус
sudo aa-status | grep -i libvirt

# 4. Проверьте права на файлы
ls -la /var/lib/libvirt/pools/dns-server/

# 5. Проверьте конфигурацию qemu.conf
grep -E "^(user|group|dynamic_ownership|security_driver)" /etc/libvirt/qemu.conf
```

## Матрица решений

| Окружение | Рекомендуемая опция | Обоснование |
|-----------|-------------------|-------------|
| Production сервер | 1 (Безопасный) | Максимальная защита |
| Публичный сервер | 1 (Безопасный) | Максимальная защита |
| Dev на личном ПК | 2 (Компромисс) | Баланс безопасности/удобства |
| WSL2 / Docker | 2 (Компромисс) | AppArmor может не работать |
| Временная отладка | 3 (Небезопасный) | Только для быстрого теста |

## Ссылки и дополнительная информация

- [QEMU Security](https://wiki.qemu.org/SecurityProcess)
- [Libvirt Security](https://libvirt.org/securityprocess.html)
- [AppArmor Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)
- [SELinux and libvirt](https://wiki.libvirt.org/page/SELinux)
- [Principle of Least Privilege](https://en.wikipedia.org/wiki/Principle_of_least_privilege)

## Восстановление после компрометации

Если вы подозреваете компрометацию VM:

```bash
# 1. Немедленно остановите VM
virsh destroy dns-server

# 2. Проверьте логи на подозрительную активность
journalctl -u libvirtd --since "1 hour ago"
sudo ausearch -m avc -ts recent

# 3. Проверьте процессы
ps auxf | grep qemu

# 4. Пересоздайте VM с безопасными настройками
terraform destroy
sudo ./scripts/fix-libvirt-permissions-safe.sh  # Опция 1
terraform apply
```

## Заключение

**Для разработки на личном компьютере:** Небезопасный или компромиссный вариант приемлем, если вы понимаете риски.

**Для production:** Используйте только безопасный вариант (опция 1), независимо от сложности настройки.

**Помните:** Безопасность - это не одноразовая настройка, а постоянный процесс мониторинга и обновления.
